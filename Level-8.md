# Bandit Level 8

## Objective
Find the unique line in a file.

## Commands Used

```bash
sort filename | uniq -u
```

## Explanation
Sorted the file before using `uniq -u` to find the line that appears only once.

## What I Learned
- Sorting data
- Finding unique values
- Combining commands with pipes
