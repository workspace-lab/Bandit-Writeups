# Bandit Level 1

## Objective
Read the password stored in a file named `-`.

## Commands Used

```bash
cat ./-
```

## Explanation
Since the filename starts with a dash, `./` tells the shell that it is a file in the current directory.

## What I Learned
- Special filenames
- Using relative paths
- Reading files with `cat`
